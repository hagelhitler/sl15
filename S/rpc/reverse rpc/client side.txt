/*
 * This is sample code generated by rpcgen.
 * These are only templates and you can use them
 * as a guideline for developing your own functions.
 */

#include "rev.h"



void stringrev_prog_1(char *host)
{
	CLIENT *clnt;
	strings  *result_1;
	strings  stringrev_1_arg;

#ifndef	DEBUG
	clnt = clnt_create (host, STRINGREV_PROG, STRINGREV_VERS, "udp");
	if (clnt == NULL) {
		clnt_pcreateerror (host);
		exit (1);
	}
#endif	/* DEBUG */

printf("Enter a string:");
	scanf("%[^\n]s", stringrev_1_arg.str);

	result_1 = stringrev_1(&stringrev_1_arg, clnt);
	if (result_1 == (strings *) NULL) {
		clnt_perror (clnt, "call failed");
	}
	
printf("Reverse String is::%s", result_1->str );


#ifndef	DEBUG
	clnt_destroy (clnt);
#endif	 /* DEBUG */
}


int main (int argc, char *argv[])
{
	char *host;
	int ch;

	if (argc < 2) {
		printf ("usage: %s server_host\n", argv[0]);
		exit (1);
	}
	host = argv[1];
	do
	{
		system("clear");
		//printf("Enter a string:");
	//scanf("%[^\n]s", stringrev_1_arg.str);
	stringrev_prog_1 (host);
	printf("\n try again[1/0]:\n");
	scanf("%d",&ch);
	}while(ch==1);
exit (0);
}